import { expect, test } from "vitest";

import day from "./index.ts";

test("sample", () => {
  const result = day([
    /* input */
  ]);

  expect(result).toMatchObject({
    part1: 0,
    part2: 0,
  });
});
